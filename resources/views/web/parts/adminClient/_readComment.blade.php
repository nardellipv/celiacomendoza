@extends('web.adminClient')@section('content')    <div class="container-fluid">        @include('web.parts.alerts.success')        <div class="row">            <div class="index-lcontent col-xl-10 offset-1">                <h4 class="tittle-w3-agileits mb-4">Comentarios Recibidos</h4>                <div class="text-center">                    @if(count($comments) == 0)                        <h5 class="card-title">Sin Mensajes</h5>                    @endif                </div>                <div class="container-fluid">                    <div class="row">                        <div class="email-list col-12">                            <ul class="collection">                                @foreach($comments as $comment)                                    @if($comment->status == 'ACTIVE')                                        <li class="collection-item d-flex">                                            <span class="circle mr-3">{{ mb_substr($comment->name, 0,1) }}</span>                                            <div class="mid-cn">                                                @if($comment->read == 'NO')                                                    <span class="email-title">{{ $comment->name }}</span>                                                @else                                                    <i><p>{{ $comment->name }}</p></i>                                                @endif                                                <p class="paragraph-agileits-w3layouts">{{ $comment->message }}</p>                                            </div>                                            <a href="#" class="email-time">                                                <small class="text-primary">{{ Date::parse($comment->created_at)->format('d/m/y H:m') }}</small>                                            </a>                                            <div>                                                <a href="{{ route('denuncia', $comment->id) }}" class="btn btn-warning">Denunciar                                                    Comentario</a>                                            </div>                                        </li>                                    @else                                        <li class="collection-item d-flex">                                            <span class="circle mr-3">{{ mb_substr($comment->name, 0,1) }}</span>                                            <div class="mid-cn">                                                @if($comment->read == 'NO')                                                    <span class="email-title">{{ $comment->name }}</span>                                                @else                                                    <i><p>{{ $comment->name }}</p></i>                                                @endif                                                <p class="paragraph-agileits-w3layouts">{{ $comment->message }}</p>                                            </div>                                            <a href="#" class="email-time">                                                <small class="text-primary">{{ Date::parse($comment->created_at)->format('d/m/y H:m') }}</small>                                            </a>                                            <div>                                                <p class="text-danger">Comentario Baneado (no se muestra en el sitio)</p>                                            </div>                                        </li>                                    @endif                                @endforeach                            </ul>                        </div>                        <!-- right-strip -->                    </div>                </div>            </div>        </div>    </div>@endsection