@extends('web.adminClient')@section('content')    <div class="outer-w3-agile mt-3">        <h4 class="tittle-w3-agileits mb-4">Productos En Pausa</h4>        <table class="table">            <thead class="thead-dark">            <tr>                <th scope="col">Foto</th>                <th scope="col">Nombre</th>                <th scope="col">Precio</th>                <th scope="col">Acción</th>            </tr>            </thead>            <tbody>            @foreach($productsDisable as $productDisable)                <tr>                    <td>                        <a href="../images/{{ $commerce->name }}-{{ $commerce->id }}/products/{{ $productDisable->photo }}"                           target="_blank"><img class="thumbnail  img-responsive"                                                src="{{ asset('images/thumbnail/products/'.($productDisable->photo)) }}"                                                width="20%"></a>                    </td>                    <td width="20%">{{ $productDisable->name }}</td>                    <td width="20%">                        $ {{ $productDisable->price }}                    </td>                    <td>                        <a href="{{ route('product.edit', $productDisable->id) }}"                           class="btn btn-primary btn-sm">                            <i class="fa fa-edit"></i> Editar                            <a href="{{ route('unavailable', $productDisable->id) }}"                               class="btn btn-info btn-sm">                                <i class=" fa fa-times"></i> Desactivar                            </a>                            {!! Form::open(['method' => 'DELETE','route' => ['product.destroy', $productDisable->id],'style'=>'display:inline']) !!}                            {{Form::token() }}                            <button type="submit" class="btn btn-danger btn-sm">                                <i class=" fa fa-trash"></i> Eliminar                            </button>                            {!! Form::Close() !!}                        </a>                    </td>                </tr>            @endforeach            </tbody>        </table>    </div>@endsection